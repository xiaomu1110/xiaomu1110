name: Card

on:
  schedule:
    - cron: "30 0 * * *"
  workflow_dispatch:

jobs:
  card:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: generate cards
        run: python scripts/card.py
        env:
          GITHUB_TOKEN: ${{ secrets.METRICS_TOKEN }}

      - name: push to output branch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          mkdir -p /tmp/cards && cp gen/* /tmp/cards/
          rm -rf gen
          git fetch origin output || true
          git checkout output 2>/dev/null || git checkout -b output
          rm -f hero-*.svg repos-*.svg specs-*.svg card.svg header.svg footer.svg github-metrics.svg github-snake*.svg
          cp -f /tmp/cards/* .
          git add -A
          git commit -m "chore: update profile cards" || echo "no changes"
          git push origin output
